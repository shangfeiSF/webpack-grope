import {a as A, b as B} from 'constants'

import * as math from 'math'

export const add_result = math.add(A, B)
export const subtract_result = math.subtract(A, B)
export const multiply_result = math.multiply(A, B)
export const divide_result = math.divide(A, B)