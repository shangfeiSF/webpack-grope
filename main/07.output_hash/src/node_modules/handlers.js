import { TEN, TWENTY } from 'numbers'

console.warn('loaded handlers.js and executed')

export default {
  homeHandler: (route) => {
    console.log(route, TEN)
  },
  aboutHandler: (route) => {
    console.log(route, TWENTY)
  }
}