import {a as A, b as B} from 'constants'

import * as math from 'math'

let add_result = math.add(A, B)
let subtract_result = math.subtract(A, B)
let multiply_result = math.multiply(A, B)
let divide_result = math.divide(A, B)

console.log(add_result)
console.log(subtract_result)
console.log(multiply_result)
console.log(divide_result)